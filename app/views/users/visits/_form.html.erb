<%= form_for(@users_visit) do |f| %>
  <% if @users_visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@users_visit.errors.count, "error") %> prohibited this users_visit from being saved:</h2>

      <ul>
      <% @users_visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
